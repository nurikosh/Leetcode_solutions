func findPeakElement(nums []int) int {
    first := 0
    last := len(nums) - 1

    for last > first{
        mid := first + (last - first)/2
        if nums[mid] > nums[mid + 1]{
            last = mid
        }else{
            first = mid + 1
        }

    }
    return last
}
